# 贡献指南

欢迎补充论文、官方实现、评测资源，以及纠正分类和失效链接。请优先提交能帮助读者理解“推理如何影响推荐决策”的内容。

## 收录范围

- 核心：参与推荐、排序、重排或交互决策的思维链、潜在推理、规划、搜索、自我修正、推理蒸馏，以及针对这些过程的强化学习。
- 评测：直接测试推荐推理、偏好约束、工具使用或交互决策能力的基准。
- 背景：理解核心工作所必需的生成式检索和物品编码方法，放入背景章节。
- 单独使用 LLM、强化学习或物品 ID 生成，不足以进入核心清单。仅在结果之后生成解释的工作，也需要说明其与决策的联系。

分类是本仓库的编辑规则，不代表领域公认定义。边界不清时，请先提交论文推荐 Issue，说明疑问。

## 提交前核实

1. 搜索论文标题和 arXiv ID，避免重复收录。预印本与会议版本一般维护为同一条。
2. 阅读作者提交的论文摘要、正文或正式出版页面。其他 Awesome、博客和搜索结果只用于发现线索。
3. 日期使用 arXiv **首次提交月份**，不以最新修订日期代替。没有 arXiv 的论文使用可核实的首次公开日期，并注明口径。
4. 会议录用信息需要正式出版页面或会议列表佐证。默认仅标 arXiv，不根据作者仓库标题推断。
5. 官方代码、模型和数据须能从论文或作者项目页追溯，且分别打开核对。未找到或无法确认时写“未核实”，不写“未开源”。
6. 方法摘要使用自己的话，不复制论文摘要。结果写成“作者报告”，涉及数值时注明数据集、指标、协议和论文版本。

## 首页条目格式

每篇论文按主要贡献归入一个专题，专题内按首次提交月份倒序排列；其他专题通过链接和标签交叉索引。

```markdown
<a id="paper-short-name"></a>
### 简称：论文完整标题

**YYYY-MM · arXiv** · `任务标签` `方法标签`

用一至两句话说明方法、推理参与决策的位置和收录理由。

[论文](论文页面地址) · [官方代码](已核实的作者仓库地址)
```

没有已核实的官方代码时，将资源行改为“论文链接 · 官方代码：未核实”。链接模板中的说明文字需要替换为真实地址，不要原样提交。

推荐标签：

- 任务：`序列推荐`、`评分预测`、`重排`、`对话推荐`、`商品检索`、`约束推荐`。
- 方法：`CoT`、`潜在推理`、`SFT`、`RL`、`GRPO`、`搜索`、`验证器`、`蒸馏`、`工具调用`。
- 属性：`多模态`、`训练期推理`、`推理期推理`、`基准`。

标签可以增补，但应优先复用现有术语。

## 阅读卡片

精选工作可在 [阅读卡片](docs/reading-notes.md) 中补充：

1. 推理的位置：训练时、预测时，还是两者都有。
2. 训练目标或奖励：优化什么，是否引入教师、额外数据或工具。
3. 证据：不推理对照、关键消融、数据集、候选规模与指标。
4. 成本：生成长度、采样数、工具调用、硬件和延迟；未知项如实记录。
5. 局限：区分作者结论与维护者解读，链接到核实的版本及章节。

只有运行了实验，才能标注“本仓库已复现”。打开代码仓库不等于完成复现。详见 [评测记录指南](docs/evaluation.md)。

## 提交流程

1. 修改 README 中对应条目；必要时更新阅读卡片和来源笔记。
2. 确认本地文件链接、目录锚点、论文标题和日期无误。
3. 使用 PR 模板说明变更和一手证据。纠错可只提供相关差异。
4. 链接检查失败时先看具体原因：404/410 需修复，429 或超时需重试和人工核实，不应直接删除仍有效的论文。

有 [lychee](https://github.com/lycheeverse/lychee) 时，可在仓库根目录运行：

```bash
lychee --offline --config .lychee.toml README.md CONTRIBUTING.md 'docs/**/*.md'
lychee --config .lychee.toml README.md CONTRIBUTING.md 'docs/**/*.md'
```

CI 在相关文件的 push、pull request 或手动触发时检查链接；不会自动提交论文或创建 Issue。链接可访问也不能代替内容核实。

## 内容许可

提交新增的原创整理文字，即表示同意按本仓库的 [CC BY 4.0 许可](LICENSE) 提供该部分内容。外链论文、代码、模型和数据保留各自许可；请勿直接搬运不具备授权的图片、全文或数据。
